import React from 'react';
import Head from 'next/head';

export default ({ children }) => (
  <Head>
    <title>
      {children} | Paulin Trognon, développeur senior JavaScript (React / Angular / Node)
    </title>
  </Head>
);
