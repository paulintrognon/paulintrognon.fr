/**
 * A link to a certain page, an anchor tag
 */

import styled from 'styled-components';

const A = styled.a`
  color: black;
  text-decoration: underline;

  &:hover {
    color: black;
  }
`;

export default A;
